# Platform app
server.port=8080

# MySQL (primary datasource). Create DB and run schema-mysql.sql before first start.
# spring.datasource.url=jdbc:mysql://localhost:3306/devhome?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.datasource.username=root
# spring.datasource.password=root123
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# Optional: H2 for local dev when MySQL is not available. Use profile: spring.profiles.active=dev-h2
# See application-dev-h2.properties

# Optional: MySQL for tech improvement task list (per-type tables). If set, GET /api/tech-improvement/tasks?type=xxx reads from MySQL.
spring.datasource.mysql.url=jdbc:mysql://localhost:3306/devhome?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
spring.datasource.mysql.username=root
spring.datasource.mysql.password=root123
# platform.openspec-work-dir=/path/to/DevHome
# Optional: LLM endpoint for design generation
# platform.llm-endpoint=http://localhost:11434/v1
# Optional: Cursor API for triggering coding
# platform.cursor-api-endpoint=
