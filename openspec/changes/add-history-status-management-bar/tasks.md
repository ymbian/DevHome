## 1. 后端状态模型与接口

- [x] 1.1 为历史需求定义统一状态视图字段：`requirement`、`design`、`specModeEnabled`、`coding`
- [x] 1.2 在历史列表接口返回中增加状态视图字段，确保每条记录可直接渲染状态条
- [x] 1.3 在需求详情接口返回中增加同样状态视图字段，保持页面一致
- [x] 1.4 为旧记录补充默认推导逻辑，避免出现空状态

## 2. Spec 模式状态采集

- [x] 2.1 在设计生成接口处理 `specMode` 参数时持久化该实例的 spec 模式开关状态
- [x] 2.2 在再次生成设计时正确覆盖 spec 模式状态，保证状态与最近操作一致
- [x] 2.3 将 spec 模式状态纳入历史接口返回，输出 `开启/关闭` 所需布尔值

## 3. 前端状态条与历史布局

- [x] 3.1 在开发首页历史区域上方新增状态条容器
- [x] 3.2 状态条按顺序显示：`需求 -> 设计 -> spec模式(开启/关闭) -> 编码`
- [x] 3.3 点击不同历史任务时刷新状态条内容，始终反映当前选中任务
- [x] 3.4 保持历史任务列表在状态条下方，原有查看与跳转行为不变

## 4. 验证

- [ ] 4.1 提交新需求后检查状态条初始状态（需求完成，其余未完成）
- [ ] 4.2 生成设计并切换 spec 模式开/关，检查状态条中 spec 状态正确变化
- [ ] 4.3 完成 openspec 后检查编码阶段状态更新，并确认历史列表展示正常
